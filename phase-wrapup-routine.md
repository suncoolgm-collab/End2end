# Phase 마무리 루틴

> 작성일: 2026-09-02 (Phase 1 종료 시점에 만듦)
> 이 문서는 **Phase 하나가 끝날 때마다 반복하는 절차**다.
> 학습 계획서(`python-backend-study-plan.md`)의 부속 문서로 쓴다.

---

## 0. 이 루틴을 쓰는 시점

Phase의 실습이 끝나고 "직접 확인할 것" 목록을 다 통과했을 때.
**개념을 다 이해했다고 느낄 때가 아니라, 손으로 돌려봤을 때**가 기준이다.

한 번에 다 하지 않아도 된다. 순서만 지킨다.

---

## 1. 확인 (10분)

Phase 시작할 때 정해둔 "직접 확인할 것" 목록을 다시 읽고 하나씩 통과시킨다.

- 통과한 것 옆에 결과를 한 줄로 적는다
- 예상과 달랐던 것에는 표시를 해둔다 → **이게 블로그 글감이다**
- 못 한 게 있으면 다음 Phase로 넘기지 말고 지금 한다

> Phase 1 예시: 서버를 끄면? 다른 포트로 접속하면? 같은 포트에 두 개 띄우면?
> 세 번째에서 예상과 다른 결과가 나왔고, 그게 그날 제일 값진 소재였다.

---

## 2. Git 정리 (10분)

- [ ] `git status`로 빠진 파일 확인
- [ ] `.gitignore`에 새로 생긴 생성물이 들어갔는지 확인 (`__pycache__`, `*.db`, `.env` 등)
- [ ] 실습 단위로 커밋. 메시지는 **나중에 이 줄만 보고 뭘 했는지 알아볼 수 있게**
- [ ] `git push`
- [ ] 커밋 링크를 복사해둔다 → 블로그에서 전체 코드 대신 이걸 건다

### 커밋 메시지 예시

```
Phase 1: 표준 라이브러리로 HTTP 서버 구현
```

"수정", "커밋" 같은 건 나중에 자기가 보고도 모른다.

---

## 3. 블로그 글 (20~30분 + 그림)

### 3-1. 글 구조 (실제로 쓰고 있는 형식)

번호 매긴 절, 짧은 단정문(`~이다` / `~한다`), 중간 정리와 마지막 정리.

```
1. 개념 A
   1.1 정의
   1.2 세부
2. 개념 B
   ...
[중간 정리]        ← 개념 파트 끝
N. 실습            ← 목표 / 찾은 것 / 확인한 것
N+1. 나의 실수     ← 추측 → 실제 원인 → 배운 것
[마지막 정리]
다음 Phase 예고
```

### 3-2. "나의 실수" 섹션이 본체다

계획서 8장의 형식은 이것이다.

> 뭘 하려고 했는가 → 어떤 에러/현상이 나왔는가 → **뭐라고 추측했는가** → 실제 원인은 무엇이었는가 → 무엇을 배웠는가

**"추측"은 반드시 직접 쓴다.** 그때 뭐라고 생각했는지는 본인만 안다.
이 부분이 비면 남는 게 정리 글밖에 없다.

한 Phase당 **1~2개**만 고른다. 다 넣으면 그 섹션만 비대해진다.
고르는 기준: 예상과 달랐던 것 > 오래 막혔던 것 > 단순 오타

### 3-3. 코드와 이미지

| 종류 | 어떻게 |
|---|---|
| 핵심 코드 | 5~15줄만 **텍스트로** |
| 전체 코드 | GitHub **커밋 링크** |
| 에러 메시지 | 텍스트 (검색될 수 있게) |
| 터미널 출력, 실행 화면, DB 조회 결과 | 스크린샷 |
| 개념 구조도 | 그림 (아래 4장) |

코드 스크린샷은 쓰지 않는다. 너무 편해서 통째로 붙이게 되고, "뭐가 핵심이지"를 거르는 단계를 건너뛰게 된다.

### 3-4. 막힌 순간 메모

**막혔을 때 그 자리에서** 에러 메시지와 추측을 한 줄 던져둔다.
하루 끝에 쓰려고 하면 기억이 안 나서 결국 요약 글로 도망가게 된다.

---

## 4. 그림

### 4-1. 언제 그리나

말로 설명하면 길어지는 것만. 보통 Phase당 2~4개.

- **구조** — 무엇이 무엇을 담고 있는가 (상속 관계, 응답의 구성)
- **흐름** — 무엇이 무엇을 부르는가 (요청 처리 순서)
- **변환** — 무엇이 무엇으로 바뀌는가 (str ↔ bytes)

개념 하나를 한 문장으로 설명할 수 있으면 그림은 필요 없다.

### 4-2. 네이버에 올릴 때

- 네이버 에디터는 **SVG를 못 받는다.** PNG로 변환해서 이미지로 올린다
- 폭 1300px 이상으로 뽑아야 선명하다
- 한글 폰트가 깨지지 않는지 확인

### 4-3. 캡션

그림 밑에 한 줄. "무엇을 보여주는 그림인가"가 아니라 **"이 그림에서 알아야 할 것"** 을 쓴다.

- 나쁨: `요청 처리 흐름도`
- 좋음: `do_GET은 직접 호출하지 않는다. 정의해두면 서버가 이름으로 찾아 호출한다.`

---

## 5. 계획서 갱신 (5분)

`python-backend-study-plan.md`의 **9장 「다음 세션 시작점」** 을 고쳐 쓴다.

- [ ] 끝난 Phase에 완료 표시 + 실제로 확인한 것 한 줄
- [ ] 다음 Phase에 할 것과 "직접 확인할 것" 목록
- [ ] 이번 Phase에서 결정된 「미정」 항목 지우기
- [ ] 새로 생긴 「미정」 항목 추가

> 계획서와 실제가 어긋난 채로 두면 계획서가 지도 역할을 못 한다.
> 형식이나 규칙을 바꿨으면 계획서도 그날 고친다.

---

## 6. 소리 내어 설명하기 (5분)

계획서 2-1의 3번. **이걸 건너뛰면 나머지가 다 의미 없다.**

이번 Phase의 핵심 개념 3~4개를 골라서 아무것도 안 보고 말로 설명한다.
막히는 지점 = 아직 이해 못 한 것. 그 부분만 다시 본다.

> Phase 1 예시:
> - `BaseHTTPRequestHandler`를 상속받으면 뭘 얻는가?
> - `do_GET`은 누가, 언제 부르는가?
> - 상속을 안 하면 왜 인스턴스조차 못 만드는가?

---

## 튜터(Claude)에게 시킬 것 / 안 시킬 것

계획서 2-2의 경계를 마무리 작업에도 그대로 적용한다.

**시킨다**
- 대화에서 나온 개념을 글 형식에 맞춰 정리
- 구조도 그리기 (SVG / PNG)
- 초안 리뷰 — 설명이 헐거운 곳에 질문 던지기
- 다음 Phase 범위 잡기

**안 시킨다**
- **「나의 실수」의 "추측" 부분** — 그때 뭐라고 생각했는지는 본인만 안다
- 아직 안 짠 코드를 미리 보여주는 것

> 다른 AI를 쓰는 것도 자유다. 다만 **거기서 뭘 해결했는지는 한 줄 알려준다.**
> 모르면 이미 아는 걸 다시 묻게 되고, 서로 시간만 쓴다.

---

## 체크리스트 (복사해서 쓰기)

```
Phase __ 마무리

[ ] "직접 확인할 것" 전부 통과, 결과 한 줄씩 기록
[ ] 예상과 달랐던 것 표시 → 글감
[ ] git status / .gitignore 확인
[ ] 커밋 + push, 커밋 링크 복사
[ ] 블로그 글 — 개념 / 중간정리 / 실습 / 나의 실수 / 마지막정리
[ ] "추측" 칸 직접 채움
[ ] 그림 PNG로 변환, 폰트 확인
[ ] 계획서 9장 갱신 (완료 표시 / 다음 Phase / 미정 항목)
[ ] 핵심 개념 3~4개 소리 내어 설명
```

---

## 부록: Phase 1 기록

| 항목 | 내용 |
|---|---|
| 기간 | 2026-09-02 (1일) |
| 실습 | 표준 라이브러리 `http.server`로 서버 띄우기 |
| 겪은 일 | `send_header` 인자 개수 / `wfile`에 str / 상속 제거 실험 / 같은 포트 두 개 |
| 글감으로 고른 것 | `wfile`에 str, 같은 포트 두 개 |
| 그림 | 응답 구조, 상속 관계, 요청 흐름, 인코딩 변환 (4개) |
| 예상과 달랐던 것 | 같은 포트에 서버 두 개를 띄웠는데 오류가 나지 않음 (`SO_REUSEADDR`, OS 차이) |
