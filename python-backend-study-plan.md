# Python 백엔드 End-to-End 학습 계획

> 작성일: 2026-09-01
> 이 문서는 진도표가 아니라 **지도**다. 순서대로 밟아나갈 체크리스트가 아니라, 지금 어디쯤 있고 아직 안 건드린 영역이 뭔지 확인하는 용도로 쓴다.

---

## 1. 현재 상태와 목표

**출발점**
- Python 문법을 조금 아는 수준
- 프로젝트는 Claude Code / Codex로만 해봤고, 코드를 직접 어떻게 짜야 하는지 모름
- CS 지식 거의 없음
- 하루 3~4시간 투자 가능

**진짜 문제**
아는 게 부족해서가 아니라 **직접 짜본 적이 없어서** 생긴 구멍이다.
지식을 몇 달 쌓아서 메워지는 게 아니라, 작더라도 한 바퀴를 완주해야 메워진다.

**목표**
기획 → 데이터 모델 설계 → 구현 → 테스트 → 배포 → 운영까지 한 바퀴를 직접 손으로 통과한다.

---

## 2. 절대 원칙

### 2-1. AI 사용 규칙

> **내가 설명할 수 없는 코드는 프로젝트에 넣지 않는다.**

1. **코드 생성 요청 금지.** 허용되는 건 개념 질문, 에러 메시지 해석, 그리고 다 짜고 난 뒤의 코드 리뷰.
2. **30분 규칙.** 막히면 30분 혼자 → 공식 문서 → 그래도 안 되면 "답 말고 힌트만".
   단, 2시간 넘게 진도가 안 나가면 답에 가까운 힌트를 받는다. 좌절이 학습이 되는 구간은 30분~1시간이고 그 이상은 손해다.
3. **하루 끝에 오늘 짠 코드를 소리 내어 설명해보기.** 막히는 줄은 이해 못 한 것 → 지우고 다시.
4. **막혔을 때는 상태를 말로 설명하는 것부터.** 뭘 하려는지, 어디까지 했는지, 뭘 시도했고 뭐가 안 됐는지. 쓰다가 스스로 답을 찾는 경우가 많다.

### 2-2. 튜터(Claude)의 역할 분담

**한다**
- 오늘 뭘 할지 범위 정하기
- 개념 설명 + **예시 코드** (문법·라이브러리 사용법은 예시로 보여줌)
- 설계 패턴은 **남의 도메인**(도서관 대출, 커피 주문 등)으로 예시
- 개념 설명 후 5분짜리 작은 연습 문제 (프로젝트와 무관한 것으로)
- 짠 코드 리뷰, 답 대신 질문 던지기
- 관례적인 것(테스트 폴더 위치, .env를 왜 커밋 안 하는지)은 그냥 알려줌
- **블로그 개념부 초안** (8장 참고). 단, 삽질부는 비워둔 채로 넘김

**안 한다**
- 함수 본문 작성
- 클래스 설계
- 완성된 폴더 구조 제시
- 지금 만들고 있는 프로젝트의 코드 예시

> 경계선: **뭘 만들어야 하는지는 튜터가 말하고, 어떻게 만들지는 내가 정한다.**

### 2-3. 설명 없이 넘어간 용어가 나오면 즉시 물어본다
모르는 걸 모른다고 말하는 게 이 방식에서 제일 중요한 동작.

---

## 3. 전체 구조: 두 바퀴

### 1바퀴 (5~6주) — "만들 줄 아는 사람"이 되기
작은 프로젝트를 **배포까지** 완주한다. 완성도가 목표가 아니라 전체 흐름을 한 번 통과하는 게 목표.

**여기서 익히는 것**
클래스·모듈·예외·타입힌트 / 계층 분리 / 테이블 설계와 기본 SQL / HTTP와 REST API / 인증 / 단위 테스트 / Git / Docker / 배포 / 환경 변수 관리

**여기서 못 만나는 것** (혼자 쓰는 작은 앱이라서 문제가 발생하지 않음)
JOIN과 정규화 / 인덱스와 성능 / 동시성 / 권한 설계 / 캐싱 / 비동기 작업 큐 / N+1 쿼리

> ⚠️ 위 목록을 보고 프로젝트를 키우고 싶어지는 게 첫 프로젝트를 죽이는 가장 흔한 방법이다. **1바퀴는 작게 완주하는 게 유일한 목표.**

### 2바퀴 (12~14주) — "제대로 만드는 법을 아는 사람"이 되기
사용자가 여러 명이고 테이블이 5~6개인 프로젝트(예: 커뮤니티 API). 1바퀴에서 못 만난 것들이 **실제 문제로 나타나고**, 그때 배워야 남는다.

### 그 이후
기존 프로젝트에 데이터/AI 기능 확장 (Pandas, 임베딩, 벡터 검색, RAG 등)

---

## 4. 개념 학습 순서

> **이론 몰아치기 금지.** "기초를 다 떼고 시작하자"로 가면 몇 주가 사라지고 거의 남지 않는다.
> **개념 하나 → 그 개념이 필요한 코드 짜기 → 다음 개념.** 며칠 단위가 아니라 **하루 안에서** 오간다.

아래로 갈수록 위의 것을 전제로 한다. 순서를 지킬 것.

### Phase 0. 프로그램이 실행된다는 것
- 파일과 프로세스의 차이 (디스크의 글자 덩어리 vs 메모리에서 돌아가는 것)
- 프로세스가 끝나면 메모리는 사라진다 → **그래서 저장소가 필요하다**
- 서버는 끝나지 않는 프로그램이다
- 터미널, 현재 경로, PATH, 가상환경이 하는 일

### Phase 1. 서버란 무엇인가
- 클라이언트와 서버, 요청과 응답, 포트
- 실습: 프레임워크 없이 표준 라이브러리로 서버 띄우고 브라우저로 접속
- 직접 확인: 서버를 끄면? 다른 포트로 접속하면? 같은 포트에 서버 두 개를 띄우면?

### Phase 2. HTTP와 API
- HTTP 메서드 (GET/POST/PUT/PATCH/DELETE)
- 상태 코드
- 요청 본문 / 응답 본문, JSON
- REST의 개념
- **"API가 뭐냐"에 대한 답이 여기서 나온다**
- 쿠키, 세션, JWT

### Phase 3. 데이터를 어디에 저장하는가
- 파일 저장 vs 데이터베이스
- 테이블, 행, 열, 기본키
- 기본 SQL: SELECT / INSERT / UPDATE / DELETE
- **ORM보다 raw SQL을 먼저**

### Phase 4. 테이블 설계
- 관계, 외래키
- JOIN, GROUP BY
- 정규화
- 인덱스, 트랜잭션
- ※ Phase 3을 쓰다가 불편함을 겪은 뒤에 와야 한다

### Phase 5. 계층 분리와 아키텍처
- 코드를 왜 나누는가
- domain / repository / service / 입출력 계층
- 의존성 주입
- ※ **가장 늦게.** 안 나눈 코드가 지저분해지는 걸 겪지 않으면 이해가 안 된다

### Phase 6. 품질과 배포
- pytest, 단위 테스트 / 통합 테스트의 차이
- 로깅, 일관된 에러 응답
- 설정을 .env로 분리
- DB 마이그레이션 (Alembic)
- Docker
- 배포 (Railway / Fly.io), SQLite → PostgreSQL
- GitHub Actions로 CI

---

## 5. 병행할 CS 공부

> CS는 **먼저**가 아니라 **병행**이다.
> "CS를 모르니까 CS부터" → "C언어를 모르네" → "컴퓨터 구조를..." 로 선수과목이 무한히 밀리는 게 최악의 시나리오.
> 인덱스 이론을 미리 읽으면 잊어버리지만, 내 조회가 3초 걸리는 걸 겪고 나서 읽으면 안 잊는다.

### A. 만들면서 그때그때 (별도 시간 배정 안 함)
터미널·파일 경로 / Git / 문자 인코딩(UTF-8) / 클라이언트-서버 / HTTP 메서드와 상태 코드 / JSON

### B. 매일 30~40분 고정
- **자료구조** — 배열/리스트, 해시테이블(딕셔너리가 왜 빠른가), 스택·큐, 시간복잡도(Big-O) 체감. 트리·그래프는 뒤로.
- **네트워크** — IP, 포트, DNS, TCP, HTTP/HTTPS. "브라우저에 주소를 치면 무슨 일이 일어나는가"를 순서대로 설명할 수 있으면 충분.
- **운영체제** — 프로세스와 스레드, 동시성. 나중에 async/await를 만나면 이게 없으면 벽에 부딪힌다.
- **DB 이론** — 인덱스가 왜 빠른지, 트랜잭션이 뭘 보장하는지. 6주차 이후 시작해도 늦지 않음.

### C. 지금은 건드리지 않는다
컴파일러 / 어셈블리 / CPU 구조 상세 / DP·그래프 알고리즘
→ 중요하지 않아서가 아니라 지금 배우면 쓸 데가 없어서 증발한다. 코딩테스트가 필요해질 때 따로.

### 교재
한 분야당 **얇은 책 한 권**. 두꺼운 전공서적은 지금 펴면 완주 못 한다.
넓게 훑고 싶으면 CS50 (분량이 있으니 프로젝트와 병행하며 천천히).

---

## 6. 1바퀴 프로젝트: 복습 스케줄러

오늘 공부한 개념을 등록하면 정해진 간격으로 복습 목록에 다시 띄워주는 도구.

**이걸 고른 이유**
- 매일 쓰게 된다 → 프로젝트를 쓰는 행위가 곧 공부
- 핵심 로직이 순수 함수 → 테스트 짜기 교과서적으로 좋음
- 날짜 처리를 정면으로 만난다 → 실무에서 실제로 버그 나는 지점
- 확장 방향이 자연스러움

### 정해진 것
- **복습 주기 규칙:** 성과 반영 방식. 복습 시 **3단계 평가**(쉬움 / 애매 / 어려움)
  - 쉬움 → 다음 단계로 / 애매 → 같은 간격 유지 / 어려움 → 1단계로 되돌리기
  - 핵심 함수: `(현재 단계, 오늘 날짜, 평가) → 다음 복습일` — 순수 함수, 테스트 케이스 10개 이상 나옴
- **마지막 단계 항목:** 졸업시키지 않고 시험이 끝날 때까지 계속 순환
- **1바퀴 기능 범위 (이게 전부):**
  1. 항목 등록 (제목 + 태그 + 메모 + 블로그 링크)
  2. 오늘의 복습 목록 조회
  3. 복습 완료 처리 (3단계 평가)
  4. 간단한 통계 (총 항목 수, 연속 학습 일수, 태그별 분포)

### 미정 — 직접 결정해야 할 것
- [ ] 단계별 간격을 며칠로 (1-3-7-14-30? 더 촘촘하게?)
- [ ] **복습 종료 시점을 어디에 붙일까**
  - (A) 항목마다 종료일 컬럼 → 단순함
  - (B) 시험 개념을 별도 테이블로, 항목이 거기 속함 → 더 쓸모 있고 JOIN을 만나게 됨. 대신 손이 한 번 더 감
  - ※ Phase 3~4 지식이 있어야 판단 가능. 그때 다시 결정
- [ ] 시험에 안 묶인 항목은? (종료 없이 계속 순환 vs 모든 항목이 시험에 속해야 함)
- [ ] 종료일이 지난 항목은? (사라짐 vs 보관함 vs 데이터는 남고 안 뜸)
- [ ] 시험이 끝났는데 계속 기억하고 싶은 개념은? (벼락치기와 장기 기억은 다른 목적)

### 반드시 부딪히게 될 문제 (오히려 이걸 만나려고 고른 것)
- **"오늘"의 기준이 몇 시인가?** 새벽 2시에 복습하면 어제 건가 오늘 건가. (학습 앱들은 보통 새벽 4시를 경계로 잡음)
- **밀린 항목 처리.** 3일 여행 다녀와서 40개가 밀렸다면, 다음 간격을 원래 예정일 기준으로? 실제 복습일 기준으로? — 정답이 없고 직접 결정해야 하는 종류의 문제

### 2바퀴로 미룰 것
복습 알림 메일/푸시 / 블로그 RSS 자동 등록 / 이미지·코드 첨부 / 태그 계층 구조 / 캘린더 뷰 / 다중 사용자 / 임베딩 기반 관련 항목 추천

### 데이터 모델
※ 초안을 받았지만 Phase 3~4를 배운 뒤 **직접 다시 설계할 것.**
최소한 "복습 이력을 남기려면 뭘 저장해야 하는가"는 스스로 답을 내볼 것.

### 기술 스택 (처음에 고정, 바꾸지 않는다)
Python + FastAPI + SQLAlchemy + SQLite(→PostgreSQL) + pytest + Docker
화면은 Jinja2 템플릿. **React는 1바퀴에 넣지 않는다** — 배우려던 것의 절반을 놓친다.

---

## 7. 하루 루틴 (3~4시간 기준)

| 시간 | 내용 |
|---|---|
| 30~40분 | **CS 공부 (B항목)** — 맨 앞에 고정 |
| 2시간+ | **직접 구현** — 여기가 본체 |
| 30분 | 삽질 / 공식 문서 |
| 40분 | 블로그 글쓰기 (개념부 검토 20 + 삽질부 작성 20) |

- CS를 맨 앞에 두는 건 그날이 통째로 공부로 넘어가지 않게 하는 안전장치
- **컨디션이 좋아도 개념 시간을 늘리지 않는다.** 구현 시간만 늘린다. 비율이 공부 쪽으로 기울면 다시 "읽기만 하는" 상태로 회귀
- 주말엔 새 개념 대신 프로젝트 진행

---

## 8. 블로그 (네이버)

### 형식 — 개념부 + 삽질부

글 하나가 두 부분으로 나뉜다.

**1. 개념부** — 그날 배운 것을 구조를 잡아 정리. 번호를 붙이고, 표를 쓰고, 다시 읽어서 복습이 되게 쓴다.
**2. 삽질부** — "나의 실수" 섹션.
> 뭘 하려고 했는가 → 어떤 에러/현상이 나왔는가 → **뭐라고 추측했는가** → 실제 원인은 무엇이었는가 → 무엇을 배웠는가

> ⚠️ **삽질부가 없는 글은 실패다.** 개념부만 있으면 교재를 옮겨 적은 것에 가까워서 남는 게 적다.
> 원인을 언어로 설명해야 어설픈 이해가 드러나고, **그 순간이 진짜 복습**이다.

### 분업 — 개념부 초안은 튜터가

매일 개념부를 처음부터 쓰면 시간이 감당이 안 되고, 결국 글쓰기를 건너뛰게 된다. 그래서 이렇게 나눈다.

- **개념부:** 튜터가 초안을 뽑는다. 그날 채팅에서 설명한 것을 재정리하는 것이라 새로 얻는 건 없고 시간만 아낀다.
- **삽질부:** **반드시 직접 쓴다.** 특히 "뭐라고 추측했는가"는 남이 쓸 수 없다.

> ⚠️ **초안을 그대로 올리면 이 방식은 무너진다.** 개념부를 받는 대가로 지켜야 할 것:
> **읽으면서 설명할 수 없는 문단은 지우거나 다시 쓴다.** 지운 자리가 곧 내일 물어볼 것이다.
> 설명할 수 없는 문단을 그대로 남기면 2-1의 첫 번째 원칙("내가 설명할 수 없는 코드는 넣지 않는다")을 글에서 어기는 것이다.

### 연재로 쓴다 — 앞 글을 반복하지 않는다

글이 쌓이면 개념이 겹치기 시작한다. 매번 처음부터 설명하면 **글마다 절반이 재탕**이 되고, 다시 읽어도 새로 얻는 게 없다.

- 겹치는 개념은 **"앞 글에서 다뤘다"고 한 줄 쓰고 넘어간다.** 다시 설명하지 않는다
- 대신 **이번에 새로 보인 각도**를 쓴다. 예: 앞 글에서 `Connection: keep-alive`를 브라우저가 보내는 요청으로 봤다면, 이번 글은 서버가 `close`를 통지하는 쪽
- 앞 글이 더 잘 쓴 부분은 그대로 두고 링크만 건다. 다시 쓰면 대개 나빠진다

> ⚠️ **튜터에게 초안을 요청할 때는 직전 글을 같이 넘긴다.** 안 그러면 겹치는 걸 모르고 다시 쓴다.
> 초안을 받으면 **앞 글과 겹치는 문단부터 지운다.** 남는 것이 오늘 실제로 새로 배운 것이다.

### 시간
- 개념부 검토 20분 + 삽질부 작성 20분 = **40분**
- 7장 루틴의 20~30분을 40분으로 늘린다. 초안이 있으므로 이 시간에 가능하다
- 40분을 넘기면 자른다. 완성도보다 매일 쓰는 게 중요

### 코드 다루기
- **핵심 코드 5~15줄만 텍스트로.** 짧게 추리는 강제성이 글을 좋게 만든다
- **스크린샷은 코드가 아닌 것에** — 에러 메시지 전문, 터미널 출력, DB 조회 결과, 실행 화면
- **전체 코드는 GitHub 링크로.** 커밋 단위로 걸면 "이 글을 쓸 때 코드가 어땠는지"까지 남아서 스크린샷보다 정확
- 코드 스크린샷을 남발하지 않는 이유: 너무 편해서 통째로 붙이게 되고, "뭐가 핵심이지"를 거르는 단계를 건너뛰게 된다

### 규칙
- **블로그 플랫폼을 직접 만들지 않는다.** 2바퀴 이후에.
- 막힌 순간에 **에러 메시지와 그때의 추측을 메모에 한 줄 던져둘 것.** 개념부는 나중에 복원되지만 **그때의 추측은 복원이 안 된다.** 이 메모가 없으면 삽질부를 못 쓴다
- 글쓰기는 40분 제한. 부담되면 주 3~4회
- 개발 글이 쌓이면 Velog 분리 고려 (마크다운, 코드 블록, 개발자 노출)

---

## 9. 다음 세션 시작점

### Phase 0 완료 (환경: Windows, PowerShell)
- **실습 1 (파일로 카운터 유지) 완주.** try/except, with, 파일 읽고 쓰기, `write()`가 글자 수를 반환한다는 것까지 확인
- **실습 2 (가상환경) 완주.** PATH가 뭔지, 활성화가 PATH 맨 앞에 `.venv\Scripts`를 끼워 넣는 것이라는 것까지

### Phase 1 완료 (2026-09-02)
- 프레임워크 없이 표준 라이브러리로 서버 띄우기 실습 완주
- 클라이언트-서버, 요청-응답, 포트 개념 확인

### Phase 2 1일차 (2026-09-05)

- HTTP 메서드 중 GET, POST 실습 (요청 본문 받아보기까지)
- 관찰: 브라우저는 GET만 보낸다 / `curl -X`가 요청의 첫 단어(메서드)를 바꾼다 / Phase 1에서 만든 서버는 메서드를 아예 읽지 않아서 GET이든 POST든 `/books`로 오든 전부 똑같은 응답이 나왔다 — 메서드는 서버가 읽고 다르게 처리하기로 정해야 비로소 의미가 생긴다는 것 확인
- PUT/PATCH/DELETE는 경로 설계와 같이 봐야 의미가 생기므로 의도적으로 다음 날로 보류

### Phase 2 2일차 (2026-09-06)

**개념 — 완료**
- REST: 기술이 아니라 관례. 경로는 명사, 행위는 HTTP 메서드
- 리소스, 컬렉션(`/books`)과 개별 항목(`/books/3`)의 구분
- 생성은 컬렉션에 보낸다. **번호는 서버가 정해서 응답으로 알려준다**
- 경로는 대상을 가리키고, 본문은 내용을 나른다
- PUT(전체 교체) vs PATCH(부분 수정), 멱등성
- **"API가 뭐냐"에 대한 답 나옴** — API / 웹 API / REST의 층 구분
- 상태 코드: 앞자리 다섯 개. 400과 405의 구분. 프로그램은 본문이 아니라 숫자로 판단한다
- HTTP 버전, `Host` 헤더, `Connection: close`
- 퍼센트 인코딩, `Content-Length`는 글자 수가 아니라 바이트 수

**실습 — 완료**
- 소켓 서버에 요청 라인 파싱 추가 (메서드·경로 추출)
- HTTP 응답 직접 조립 후 `sendall`로 전송
- GET / POST / DELETE 분기, 그 외 메서드에 **405 Method Not Allowed**
- 구조: 분기에서는 본문만 정하고, 조립·전송은 분기 바깥에서 한 번만

**삽질 (블로그 소재)**
- **파일을 고쳤는데 서버 재시작을 안 함.** GET만 응답이 오고 나머지는 `Empty reply`. 결정적 단서는 에러가 아니라 **GET 응답 본문이 코드상 나올 수 없는 값이었다는 것**
- 타입 섞임 — 문자열과 바이트를 `+`로 붙이려 함, `encode()`를 바이트에 호출

**아직 (Phase 2 남은 것)**
- 응답 본문 형식화 / JSON — 다음 세션
- 쿠키, 세션, JWT

**다음 세션 첫 마디**
- 지금 서버는 뭘 요청해도 200 아니면 405다. `/books/999` 같은 없는 것에 **404를 주려면 뭐가 있는지 알아야 하고, 그러려면 저장소가 필요하다** → Phase 3으로 가는 문

### CS 병행 (5장 B항목)
- **책을 사지 않기로 함.** 대신 `cs-목차.md`를 지도로 쓴다. 하루 30~40분, 한 항목씩 검색해서 채움
- 순서는 계획서 5장과 달리 **네트워크부터.** Phase 2가 통째로 HTTP라서 지금 제일 잘 붙음
- 읽고 나면 목차 파일에 한 줄 요약을 적는다. 요약이 안 되면 이해 못 한 것

### Git
- 저장소: https://github.com/suncoolgm-collab/End2end
- `.gitignore` 설정 (Python 표준 목록)
- 실습 단위로 커밋. 블로그에 코드를 넣을 때는 커밋 링크로 (8장 참고)
- `add` / `commit` / `push`는 직접 칠 것. 개념 설명은 5장 A항목대로 Phase 1에서 필요해질 때
- **미정:** `count.txt`처럼 실행할 때마다 내용이 바뀌는 파일을 커밋할 것인가

---

## 부록: 자주 빠지는 함정

| 함정 | 실제로 일어나는 일 |
|---|---|
| "기초부터 완벽하게 하고 시작하자" | 3개월 뒤에도 코드 한 줄 안 짬 |
| "이왕 하는 거 기능 좀 더 넣자" | 배포까지 못 감. 4주차에 방치 |
| "React도 같이 배우면 좋잖아" | 추상화가 동시에 3개 쌓여서 아무것도 안 남음 |
| "막혔는데 시간 아까우니까 AI한테" | 원래 상태로 복귀 |
| "Docker 주간이니까 Docker 공부" | 배포할 게 없으면 명령어만 외우고 끝 |
| "오늘은 개념이 재밌으니 공부만" | 다시 읽기만 하는 사람이 됨 |
| "블로그 개념부 초안 받았으니 그대로 올리자" | 읽은 적 없는 글이 내 블로그에 쌓임. 8장이 무너짐 |
| "상태 코드/문법을 미리 정리해두자" | 표만 외우고 정작 고를 때 또 검색함 |
